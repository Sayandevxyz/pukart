'use client'

import Image from 'next/image'
import { AnimatePresence, motion } from 'framer-motion'
import { useMemo, useState } from 'react'
import { Bell, Bookmark, ChevronRight, Heart, Menu, MessageCircle, Plus, Search, ShieldCheck, SlidersHorizontal, Sparkles, UserRound, X } from 'lucide-react'

const listings = [
  { id: 1, title: 'Casio fx-991ES Plus', price: 420, category: 'Academic', condition: 'Like new', type: 'Buy', seller: 'Aarav Menon', location: 'Kalapet', image: '/images/listing-laptop.png', verified: true },
  { id: 2, title: 'Single-speed city cycle', price: 2800, category: 'Transport', condition: 'Good', type: 'Buy', seller: 'Nikhil R', location: 'Hostel Road', image: '/images/listing-cycle.png', verified: true },
  { id: 3, title: 'The Artist\'s Way', price: 280, category: 'Academic', condition: 'Good', type: 'Buy', seller: 'Mara Studio', location: 'Auroville', image: '/images/listing-books.png', verified: true },
  { id: 4, title: 'MacBook stand + desk kit', price: 650, category: 'Electronics', condition: 'Like new', type: 'Rent', seller: 'Priya S', location: 'Mahatma Gandhi Road', image: '/images/listing-laptop.png', verified: true },
  { id: 5, title: 'Hand-painted tote bag', price: 380, category: 'Fashion', condition: 'New', type: 'Buy', seller: 'Ishita K', location: 'Lawspet', image: '/images/listing-books.png', verified: true },
  { id: 6, title: 'Badminton racquet pair', price: 900, category: 'Sports', condition: 'Good', type: 'Buy', seller: 'Dev Shah', location: 'Tagore Nagar', image: '/images/listing-cycle.png', verified: true },
]
const categories = ['All', 'Academic', 'Electronics', 'Hostel', 'Transport', 'Fashion', 'Sports', 'Food', 'Services']

export default function Page() {
  const [query, setQuery] = useState('')
  const [category, setCategory] = useState('All')
  const [type, setType] = useState('All')
  const [saved, setSaved] = useState<number[]>([])
  const [selected, setSelected] = useState<(typeof listings)[number] | null>(null)
  const [panel, setPanel] = useState<'sell' | 'messages' | 'profile' | 'notifications' | null>(null)
  const [menu, setMenu] = useState(false)
  const [notice, setNotice] = useState('')
  const [sort, setSort] = useState('Newest')
  const [formSent, setFormSent] = useState(false)

  const filtered = useMemo(() => {
    const result = listings.filter((item) => {
      const haystack = `${item.title} ${item.category} ${item.seller} ${item.location}`.toLowerCase()
      return (category === 'All' || item.category === category) && (type === 'All' || item.type === type) && haystack.includes(query.toLowerCase())
    })
    if (sort === 'Price low-high') return [...result].sort((a, b) => a.price - b.price)
    if (sort === 'Price high-low') return [...result].sort((a, b) => b.price - a.price)
    return result
  }, [category, query, sort, type])

  function toast(message: string) { setNotice(message); window.setTimeout(() => setNotice(''), 2600) }
  function toggleSave(id: number) { setSaved((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]) }

  return (
    <main className="min-h-screen bg-background text-foreground">
      <AnimatePresence>{notice && <motion.div initial={{ y: 20, opacity: 0 }} animate={{ y: 0, opacity: 1 }} exit={{ y: 20, opacity: 0 }} role="status" className="fixed bottom-5 left-1/2 z-50 -translate-x-1/2 rounded-full bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground shadow-xl">{notice}</motion.div>}</AnimatePresence>
      <header className="sticky top-0 z-30 border-b border-border bg-background/90 backdrop-blur-xl">
        <div className="mx-auto flex max-w-7xl items-center gap-5 px-5 py-4 lg:px-8">
          <a href="#top" className="shrink-0 font-serif text-2xl font-semibold tracking-tight text-primary">PU<span className="text-accent">K</span>art</a>
          <div className="relative hidden flex-1 max-w-lg md:block"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground" size={17} /><input aria-label="Search marketplace" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search calculators, cycles, books..." className="h-11 w-full rounded-xl border border-border bg-card pl-11 pr-4 text-sm outline-none focus:ring-4 focus:ring-accent/20" /></div>
          <nav className="ml-auto hidden items-center gap-1 md:flex"><button onClick={() => setPanel('notifications')} aria-label="Notifications" className="relative rounded-xl p-2.5 text-muted-foreground hover:bg-muted"><Bell size={19} /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-accent" /></button><button onClick={() => setPanel('messages')} aria-label="Messages" className="rounded-xl p-2.5 text-muted-foreground hover:bg-muted"><MessageCircle size={19} /></button><button onClick={() => setPanel('profile')} className="flex items-center gap-2 rounded-xl px-3 py-2 text-sm font-semibold hover:bg-muted"><UserRound size={17} /> Profile</button><button onClick={() => { setPanel('sell'); setFormSent(false) }} className="ml-2 flex items-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90"><Plus size={16} /> Sell an item</button></nav>
          <button className="ml-auto rounded-xl p-2 md:hidden" aria-label="Open menu" onClick={() => setMenu(!menu)}>{menu ? <X size={22} /> : <Menu size={22} />}</button>
        </div>
        {menu && <div className="border-t border-border px-5 pb-5 md:hidden"><div className="relative mt-4"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-muted-foreground" size={17} /><input aria-label="Search marketplace mobile" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search the marketplace" className="h-11 w-full rounded-xl border border-border bg-card pl-11 pr-4 text-sm" /></div><button onClick={() => { setPanel('sell'); setFormSent(false); setMenu(false) }} className="mt-3 w-full rounded-xl bg-primary py-3 text-sm font-semibold text-primary-foreground">+ Sell an item</button></div>}
      </header>

      <section id="top" className="border-b border-border bg-primary text-primary-foreground"><div className="mx-auto grid max-w-7xl items-center gap-10 px-5 py-14 lg:grid-cols-[1.05fr_.95fr] lg:px-8 lg:py-20"><div><div className="mb-5 inline-flex items-center gap-2 rounded-full border border-primary-foreground/20 bg-primary-foreground/10 px-3 py-1.5 text-xs font-semibold uppercase tracking-[.16em] text-accent"><ShieldCheck size={14} /> Verified student marketplace</div><h1 className="max-w-2xl font-serif text-5xl leading-[1.02] tracking-tight sm:text-7xl">Campus finds,<br /><em className="text-accent">made personal.</em></h1><p className="mt-6 max-w-xl text-base leading-7 text-primary-foreground/70 sm:text-lg">Buy, sell and rent from people you can trust at Pondicherry University. Good things are already nearby.</p><div className="mt-8 flex flex-wrap gap-3"><a href="#marketplace" className="rounded-xl bg-accent px-5 py-3 text-sm font-semibold text-accent-foreground hover:opacity-90">Explore marketplace <ChevronRight className="ml-1 inline" size={16} /></a><button onClick={() => setPanel('sell')} className="rounded-xl border border-primary-foreground/20 px-5 py-3 text-sm font-semibold hover:bg-primary-foreground/10">List something</button></div><div className="mt-9 flex items-center gap-3 text-sm text-primary-foreground/60"><div className="flex -space-x-2"><span className="flex h-8 w-8 items-center justify-center rounded-full border-2 border-primary bg-accent text-xs font-bold text-accent-foreground">A</span><span className="flex h-8 w-8 items-center justify-center rounded-full border-2 border-primary bg-muted text-xs font-bold text-primary">P</span><span className="flex h-8 w-8 items-center justify-center rounded-full border-2 border-primary bg-secondary text-xs font-bold text-primary">R</span></div> 2,400+ verified students</div></div><div className="relative overflow-hidden rounded-3xl border border-primary-foreground/15 bg-primary-foreground/10 p-3"><Image src="/images/listing-books.png" alt="Creative books and campus marketplace finds" width={900} height={700} className="h-[300px] w-full rounded-2xl object-cover sm:h-[390px]" priority /><div className="absolute bottom-7 left-7 right-7 flex items-center justify-between rounded-xl bg-card px-4 py-3 text-primary shadow-xl"><div><p className="text-[10px] font-bold uppercase tracking-widest text-accent">Campus pick</p><p className="mt-1 font-serif text-lg">Things worth keeping</p></div><Bookmark className="text-accent" size={20} /></div></div></div></section>

      <section id="marketplace" className="mx-auto max-w-7xl px-5 py-12 lg:px-8 lg:py-16"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-accent">Fresh on campus</p><h2 className="mt-2 font-serif text-4xl tracking-tight text-primary sm:text-5xl">Find your next thing.</h2></div><button onClick={() => toast('Showing all active listings')} className="flex items-center gap-2 text-sm font-semibold text-primary">View all <ChevronRight size={16} /></button></div><div className="mt-8 flex gap-2 overflow-x-auto pb-2">{categories.map((item) => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full border px-4 py-2 text-sm font-semibold transition ${category === item ? 'border-primary bg-primary text-primary-foreground' : 'border-border bg-card text-muted-foreground hover:text-foreground'}`}>{item}</button>)}</div><div className="mt-6 flex flex-col gap-3 rounded-2xl border border-border bg-card p-3 sm:flex-row sm:items-center"><div className="flex flex-1 items-center gap-3 px-2 text-sm text-muted-foreground"><SlidersHorizontal size={17} /><span>Filter by</span><select value={type} onChange={(e) => setType(e.target.value)} className="rounded-lg border-0 bg-muted px-3 py-2 text-sm font-semibold text-foreground outline-none"><option>All</option><option>Buy</option><option>Rent</option><option>Free</option></select></div><select aria-label="Sort listings" value={sort} onChange={(e) => setSort(e.target.value)} className="rounded-lg border-border bg-muted px-3 py-2 text-sm font-semibold outline-none"><option>Newest</option><option>Price low-high</option><option>Price high-low</option></select></div><p className="mt-7 text-sm text-muted-foreground">{filtered.length} items near Pondicherry University</p><div className="mt-4 grid gap-x-5 gap-y-9 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((item, index) => <motion.article initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .05 }} key={item.id} className="group"><div className="relative overflow-hidden rounded-2xl bg-muted"><button onClick={() => setSelected(item)} className="block w-full text-left"><Image src={item.image} alt={item.title} width={700} height={520} className="aspect-[4/3] w-full object-cover transition duration-500 group-hover:scale-105" /><span className="absolute left-3 top-3 rounded-full bg-card/90 px-3 py-1 text-xs font-bold text-primary">{item.type}</span></button><button aria-label={`Save ${item.title}`} onClick={() => { toggleSave(item.id); toast(saved.includes(item.id) ? 'Removed from saved' : 'Saved to favorites') }} className="absolute right-3 top-3 rounded-full bg-card/90 p-2.5 text-primary shadow-sm hover:bg-card"><Heart size={17} fill={saved.includes(item.id) ? 'currentColor' : 'none'} /></button></div><button onClick={() => setSelected(item)} className="block w-full pt-4 text-left"><div className="flex items-start justify-between gap-3"><div><h3 className="font-serif text-xl text-primary">{item.title}</h3><p className="mt-1 text-sm text-muted-foreground">{item.condition} · {item.location}</p><p className="mt-3 flex items-center gap-1.5 text-xs font-semibold text-muted-foreground">{item.seller} {item.verified && <ShieldCheck size={14} className="text-accent" />}</p></div><p className="text-lg font-bold text-primary">₹{item.price.toLocaleString('en-IN')}</p></div></button></motion.article>)}</div>{filtered.length === 0 && <div className="rounded-2xl border border-dashed border-border py-16 text-center text-muted-foreground">No finds match your search yet.</div>}</section>

      <section className="border-y border-border bg-accent/10"><div className="mx-auto flex max-w-7xl flex-col gap-6 px-5 py-12 sm:flex-row sm:items-center sm:justify-between lg:px-8"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-accent">Pass it on</p><h2 className="mt-2 font-serif text-4xl text-primary">Your next buyer is here.</h2><p className="mt-3 max-w-lg leading-7 text-muted-foreground">Turn your unused things into someone else&apos;s best campus find.</p></div><button onClick={() => setPanel('sell')} className="w-fit rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Start selling <Plus className="ml-1 inline" size={16} /></button></div></section>
      <footer className="mx-auto flex max-w-7xl flex-col gap-4 px-5 py-8 text-sm text-muted-foreground sm:flex-row sm:items-center sm:justify-between lg:px-8"><p className="font-serif text-xl text-primary">PU<span className="text-accent">K</span>art</p><p>Independent student marketplace. Built for campus life.</p><p className="max-w-xs text-xs leading-5">PUKart is an independent student marketplace and is not officially affiliated with Pondicherry University.</p></footer>

      <AnimatePresence>{selected && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-end justify-center bg-primary/50 p-0 backdrop-blur-sm sm:items-center sm:p-6" role="dialog" aria-modal="true" aria-label="Product details" onClick={() => setSelected(null)}><motion.div initial={{ y: 30 }} animate={{ y: 0 }} className="relative grid max-h-[92vh] w-full max-w-3xl overflow-auto rounded-t-3xl bg-card sm:grid-cols-2 sm:rounded-3xl" onClick={(e) => e.stopPropagation()}><button aria-label="Close product details" onClick={() => setSelected(null)} className="absolute right-4 top-4 z-10 rounded-full bg-card/90 p-2 text-primary shadow"><X size={19} /></button><Image src={selected.image} alt={selected.title} width={800} height={650} className="h-64 w-full object-cover sm:h-full sm:min-h-[480px]" /><div className="flex flex-col p-7 sm:p-9"><div className="flex items-center justify-between"><span className="rounded-full bg-accent/15 px-3 py-1 text-xs font-bold text-accent">{selected.type}</span><span className="text-sm text-muted-foreground">{selected.location}</span></div><h2 className="mt-6 font-serif text-4xl leading-tight text-primary">{selected.title}</h2><p className="mt-2 flex items-center gap-1.5 text-sm text-muted-foreground">{selected.seller} <ShieldCheck size={15} className="text-accent" /></p><p className="mt-7 text-3xl font-bold text-primary">₹{selected.price.toLocaleString('en-IN')}</p><div className="mt-6 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-muted p-3"><p className="text-xs text-muted-foreground">Condition</p><p className="mt-1 font-semibold">{selected.condition}</p></div><div className="rounded-xl bg-muted p-3"><p className="text-xs text-muted-foreground">Category</p><p className="mt-1 font-semibold">{selected.category}</p></div></div><p className="mt-6 leading-7 text-muted-foreground">Verified students can connect directly to arrange a safe campus handoff. Ask a question or make an offer.</p><div className="mt-auto flex flex-col gap-3 pt-8"><button onClick={() => { setSelected(null); setPanel('messages'); toast('Conversation started') }} className="rounded-xl bg-primary py-3.5 text-sm font-semibold text-primary-foreground">Contact seller</button><button onClick={() => { toggleSave(selected.id); toast('Saved to favorites') }} className="rounded-xl border border-border py-3.5 text-sm font-semibold text-primary">{saved.includes(selected.id) ? 'Saved' : 'Save listing'}</button></div></div></motion.div></motion.div>}</AnimatePresence>
      <AnimatePresence>{panel && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-end justify-center bg-primary/45 p-0 backdrop-blur-sm sm:items-center sm:p-6" role="dialog" aria-modal="true" onClick={() => setPanel(null)}><motion.div initial={{ y: 30 }} animate={{ y: 0 }} className="w-full max-w-xl rounded-t-3xl bg-card p-7 shadow-2xl sm:rounded-3xl sm:p-9" onClick={(e) => e.stopPropagation()}>{panel === 'sell' && <SellForm sent={formSent} setSent={setFormSent} onDone={() => setPanel(null)} />} {panel === 'messages' && <Panel title="Messages" eyebrow="Your inbox" onClose={() => setPanel(null)}><div className="rounded-2xl border border-border p-4"><div className="flex justify-between"><p className="font-semibold text-primary">Mara Studio</p><span className="rounded-full bg-accent/15 px-2 py-1 text-xs font-bold text-accent">New</span></div><p className="mt-2 text-sm leading-6 text-muted-foreground">Thanks for your interest. I can answer questions about pickup.</p></div><button onClick={() => toast('Message composer opened')} className="mt-5 w-full rounded-xl bg-primary py-3 text-sm font-semibold text-primary-foreground">Open conversation</button></Panel>}{panel === 'notifications' && <Panel title="Notifications" eyebrow="You&apos;re all caught up"><p className="leading-7 text-muted-foreground">New activity from your saved items and conversations will appear here.</p></Panel>}{panel === 'profile' && <Panel title="Your profile" eyebrow="PUKart member"><div className="flex items-center gap-4 rounded-2xl bg-muted p-4"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-primary text-primary-foreground"><UserRound size={21} /></div><div><p className="font-semibold text-primary">Verified student</p><p className="text-sm text-muted-foreground">Complete your profile to start selling.</p></div></div><button onClick={() => { setPanel('sell'); setFormSent(false) }} className="mt-5 w-full rounded-xl bg-primary py-3 text-sm font-semibold text-primary-foreground">Create a listing</button></Panel>}</motion.div></motion.div>}</AnimatePresence>
      <button aria-label="Sell an item" onClick={() => setPanel('sell')} className="fixed bottom-5 right-5 z-20 flex items-center gap-2 rounded-full bg-accent px-5 py-3.5 text-sm font-bold text-accent-foreground shadow-xl md:hidden"><Plus size={18} /> Sell</button>
    </main>
  )
}

function Panel({ title, eyebrow, children, onClose }: { title: string; eyebrow: string; children: React.ReactNode; onClose?: () => void }) { return <div><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-accent">{eyebrow}</p><h2 className="mt-2 font-serif text-4xl text-primary">{title}</h2></div><button aria-label="Close panel" className="rounded-full p-2 text-primary hover:bg-muted" onClick={onClose}><X size={19} /></button></div><div className="mt-7">{children}</div></div> }

function SellForm({ sent, setSent, onDone }: { sent: boolean; setSent: (value: boolean) => void; onDone: () => void }) { if (sent) return <div className="py-8 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-accent/15 text-accent"><Sparkles size={25} /></div><h2 className="mt-5 font-serif text-3xl text-primary">Listing submitted.</h2><p className="mx-auto mt-3 max-w-sm leading-7 text-muted-foreground">We&apos;ll review your item and notify you when it is live on campus.</p><button onClick={onDone} className="mt-6 rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Done</button></div>; return <div><p className="text-xs font-bold uppercase tracking-[.18em] text-accent">Start selling</p><h2 className="mt-2 font-serif text-4xl text-primary">List an item.</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Reach verified students looking for their next useful thing.</p><form onSubmit={(e) => { e.preventDefault(); setSent(true) }} className="mt-6 flex flex-col gap-4"><input required aria-label="Listing title" placeholder="What are you selling?" className="h-12 rounded-xl border border-border bg-background px-4 text-sm outline-none focus:ring-4 focus:ring-accent/20" /><textarea required aria-label="Listing description" placeholder="Tell buyers a little about it..." className="min-h-28 rounded-xl border border-border bg-background p-4 text-sm outline-none focus:ring-4 focus:ring-accent/20" /><div className="grid gap-4 sm:grid-cols-2"><input required type="number" aria-label="Price" placeholder="Price in ₹" className="h-12 rounded-xl border border-border bg-background px-4 text-sm" /><select aria-label="Category" className="h-12 rounded-xl border border-border bg-background px-4 text-sm"><option>Academic</option><option>Electronics</option><option>Hostel</option><option>Fashion</option></select></div><button type="button" onClick={() => window.setTimeout(() => alert('AI suggestion ready: keep it clear, specific, and honest.'), 0)} className="inline-flex items-center justify-center gap-2 rounded-xl border border-accent/40 py-3 text-sm font-semibold text-accent"><Sparkles size={16} /> Generate description with AI</button><button className="rounded-xl bg-primary py-3.5 text-sm font-semibold text-primary-foreground">Submit listing</button></form></div>}

function _unused() { return null }
